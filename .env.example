# PostgreSQL config for Google Cloud / production
DB_ENGINE=django.db.backends.postgresql
DB_NAME=bodega_db
DB_USER=postgres
DB_PASSWORD=tu_password_real
DB_HOST=localhost
DB_PORT=5432

# Local fallback (set USE_SQLITE=1 if you want SQLite for local tests)
USE_SQLITE=0
